# Minecraft Forge 模块化桌游桌面

## 1. 文档信息

- 文档状态：初稿
- 项目类型：Minecraft Forge 模组
- 核心方向：规则中立的多人桌游桌面运行时
- 当前重点：先实现卡牌桌面，不实现具体桌游规则
- 目标读者：核心模组开发者、桌游内容模组开发者、测试者

本文档描述项目的产品边界、技术架构、数据模型、扩展接口和分阶段开发计划。具体类名、包名和 Minecraft/Forge 版本可在项目初始化时确定，但不得改变本文档规定的职责边界。

## 2. 项目定位

本项目在 Minecraft 多人世界中提供一个共享的桌游桌面。玩家通过放置“牌桌”创建或进入桌面，在桌面上摆放、查看、移动、翻面、旋转、洗牌和抽取卡牌。

核心模组只模拟桌面上的物理对象和信息显示，不理解具体桌游规则。

### 2.1 核心原则

1. **规则中立**：不判断某张牌是否可以打、不判断回合、不判断胜负、不计算分数。
2. **玩家自治**：玩家自行约定并执行桌游规则、流程和结果。
3. **可插拔内容**：卡牌、牌背、牌组和开局布局由独立内容模组提供。
4. **状态可靠**：服务端负责保存和同步桌面状态，保证正常操作不会因客户端差异而失步。
5. **隐藏信息是体验功能**：正常使用时按可见性显示牌面；不承诺对恶意客户端提供安全级别的防作弊保护。
6. **自由编辑可选**：牌桌可以开启自由编辑，允许参与者创建、复制、删除或任意移动卡牌。

### 2.2 核心模组不负责的内容

- 桌游规则和规则脚本
- 合法性判定
- 回合管理
- 胜负判定和计分
- 作弊检测、作弊阻止和争议仲裁
- 玩家是否诚实
- 某个具体桌游的官方规则实现

## 3. 术语

| 术语 | 含义 |
| --- | --- |
| 牌桌 | 世界中的牌桌方块及其关联的服务端桌面状态 |
| 桌面实例 | 一个正在世界中存在、可被多个玩家加入的共享桌面 |
| 卡牌定义 | 描述某种牌是什么以及如何显示的静态内容，例如 `standard:ace_of_spades` |
| 卡牌实例 | 桌面中的一张具体卡牌对象，拥有自己的实例 ID、位置和状态 |
| 牌堆 | 有顺序的卡牌实例集合，可进行洗牌、抽牌和发牌 |
| 区域 | 桌面中的逻辑空间，例如公共区、弃牌区、手牌区 |
| 玩家视图 | 针对某个玩家生成的桌面状态，可包含与其他玩家不同的隐藏信息 |
| 内容模组 | 依赖核心 API、提供卡牌和牌组等内容的 Forge 模组 |
| 自由编辑 | 允许任意修改桌面对象的牌桌模式，不代表核心负责判断修改是否合理 |

## 4. 总体架构

建议拆分为以下模块：

```text
桌游 API 模组（table-api）
        ↑
桌游核心模组（table-core）
        ↑
标准扑克牌等内容模组（table-standard-cards）
```

### 4.1 `table-api`

提供外部内容模组编译和运行所需的稳定接口：

- 卡牌定义注册接口
- 牌组模板注册接口
- 牌桌布局定义
- 卡牌和牌堆的只读模型
- 内容模组版本能力查询
- 可选的渲染和交互扩展点

API 不应暴露核心内部的网络包、NBT 细节或 GUI 实现细节。

### 4.2 `table-core`

负责运行时：

- 牌桌方块和牌桌物品
- 牌桌实例生命周期
- 卡牌实例、牌堆和区域
- 服务端状态保存
- 客户端视图同步
- 桌面交互和基础渲染
- 可见性和桌面操作权限
- 默认的创建、加入、退出和重置流程

### 4.3 内容模组

内容模组只提供桌游素材和可选的准备流程：

- 卡牌定义
- 卡牌正面、牌背纹理
- 牌组模板
- 默认桌面区域和布局
- 可选的卡牌特殊渲染
- 可选的非规则性快捷操作

内容模组不得假定核心会替它执行回合、判定出牌或宣布结果。

## 5. 牌桌实例

### 5.1 世界表示

玩家使用牌桌物品后，在世界中放置牌桌方块。牌桌方块关联一个桌面实例；桌面实例由服务端保存，客户端只保存必要的显示状态。

牌桌方块实体或等价的持久化对象至少需要保存：

- 桌面实例 ID
- 牌桌配置
- 所有卡牌实例、牌堆和区域
- 玩家参与关系
- 桌面对象位置和朝向
- 可见性配置
- 操作权限配置
- 数据版本

牌桌物品不直接承载正在进行的完整牌局。它可以用于创建新牌桌、复制配置或保存牌组模板。

### 5.2 牌桌生命周期

```text
放置牌桌
  -> 创建桌面实例
  -> 初始化空桌面
  -> 玩家加入
  -> 加载牌组或手动放置卡牌
  -> 多人操作并同步
  -> 保存世界或牌桌
  -> 牌桌被拆除/清空/删除
```

拆除牌桌时，必须明确处理桌面中的卡牌实例：

- 默认保留桌面状态并转移到牌桌物品/掉落物；或
- 仅允许在桌面为空时拆除；或
- 由牌桌配置决定。

MVP 建议：牌桌被拆除前要求桌面为空，避免第一版就引入复杂的物品承载和恢复逻辑。

## 6. 卡牌对象模型

### 6.1 卡牌定义

卡牌定义是由内容模组注册的静态数据。建议使用资源位置作为稳定 ID：

```text
standard:ace_of_spades
standard:card_back_red
```

建议字段：

```text
id              资源位置
name            显示名称
front_texture   正面纹理
back_texture    默认牌背纹理
tags            可选分类标签
size            显示尺寸或宽高比
```

卡牌定义不包含“可不可以打”“属于哪一回合”等规则信息。

### 6.2 卡牌实例

每张进入桌面的牌都创建一个卡牌实例：

```text
instance_id     唯一实例标识
card_definition 卡牌定义 ID
location        所在牌堆、区域或玩家手牌
position        桌面坐标
rotation        旋转角度
face_up         是否显示正面
visibility      可见性策略
custom_data     内容模组可选数据
```

保留实例 ID 的目的，是准确表示状态和渲染对象，而不是阻止玩家复制卡牌。自由编辑模式下复制卡牌时，复制结果应获得新的实例 ID。

### 6.3 位置模型

卡牌位置必须能表达以下状态之一：

```text
桌面自由位置
牌堆中的某个位置
某个区域中的位置
某个玩家的手牌
暂存/拖拽状态
```

建议不要把“玩家手牌”直接存入 Minecraft 原版物品栏。手牌是牌桌内部的卡牌集合，必要时可以提供单独的牌桌手牌区域 UI。

## 7. 牌堆、区域和桌面布局

### 7.1 牌堆

牌堆是有顺序的卡牌实例集合，至少支持：

- 创建牌堆
- 从牌组模板生成牌堆
- 洗牌
- 抽取顶部或底部卡牌
- 将卡牌放回牌堆顶部或底部
- 将多张牌合并为牌堆
- 将牌堆拆为单牌或多个牌堆
- 查看牌堆数量
- 按权限查看牌堆顶牌

核心只提供这些物理操作，不解释操作是否符合具体游戏规则。

### 7.2 区域

区域是桌面中的命名空间，可用于整理卡牌并定义默认可见性：

```text
公共区
弃牌区
抽牌区
玩家 A 手牌区
玩家 B 手牌区
计分区
自定义区域
```

区域不携带规则含义。名为“弃牌区”的区域不会自动禁止抽取，也不会自动触发任何事件。

### 7.3 桌面布局

桌面布局由区域、初始位置和显示规则组成。内容模组可提供开局模板，例如：

```text
standard:empty_table
standard:single_deck
standard:two_deck
```

模板只负责准备桌面，不负责开始或推进牌局。

## 8. 可见性与玩家视图

服务端保存完整桌面状态，根据玩家身份和可见性生成玩家视图。

### 8.1 建议的可见性级别

- `PUBLIC_FACE`：所有人可见牌面
- `PUBLIC_BACK`：所有人可见牌背
- `OWNER_ONLY`：仅卡牌持有者可见牌面
- `PLAYER_LIST`：仅指定玩家可见牌面
- `TABLE_OWNER_ONLY`：仅牌桌拥有者可见牌面

可见性是桌游体验所需的显示机制，不是恶意客户端安全边界。服务端应避免在正常网络消息中向无权限玩家发送明牌数据，但项目文档不承诺防止客户端端调试或其他技术性读取。

### 8.2 视图同步原则

- 服务端是桌面状态的唯一权威来源。
- 客户端发送操作请求，不直接提交完整桌面状态。
- 服务端执行操作后，向受影响玩家发送差异或新的玩家视图。
- 不同玩家收到的状态可以不同，尤其是隐藏手牌和牌堆顶牌。
- 任何状态变更都应携带桌面实例 ID和状态版本，便于丢包重试和客户端更新。

## 9. 操作模型

核心操作应描述“对桌面对象做什么”，而不是描述桌游规则。

### 9.1 MVP 操作

- `JOIN_TABLE`：加入牌桌
- `LEAVE_TABLE`：离开牌桌
- `MOVE_CARD`：移动卡牌
- `ROTATE_CARD`：旋转卡牌
- `FLIP_CARD`：翻面
- `DRAW_CARD`：从牌堆取牌
- `DEAL_CARDS`：批量发牌
- `SHUFFLE_STACK`：洗牌
- `PLACE_CARD`：将牌放到区域或桌面
- `CREATE_STACK`：创建牌堆
- `MERGE_STACK`：合并牌堆
- `SPLIT_STACK`：拆分牌堆
- `REVEAL_CARD`：改变牌的可见性
- `RESET_TABLE`：清空或按模板重置桌面

### 9.2 自由编辑操作

当牌桌启用自由编辑时，可以额外支持：

- `CREATE_CARD`：凭卡牌定义创建新实例
- `DUPLICATE_CARD`：复制已有卡牌
- `DELETE_CARD`：删除卡牌实例
- `PEEK_CARD`：查看本来不可见的牌
- `REORDER_STACK`：任意调整牌堆顺序
- `SET_CARD_STATE`：批量修改牌面、位置、朝向和可见性

这些操作不应被描述为“作弊功能”，而应被视为沙盒桌面编辑能力。是否开放由牌桌创建者和玩家自行决定。

### 9.3 操作处理流程

```text
客户端发起操作请求
  -> 服务端检查桌面实例是否存在
  -> 服务端检查请求数据格式
  -> 服务端检查玩家是否具有该操作权限
  -> 服务端修改桌面状态
  -> 服务端递增状态版本
  -> 保存或标记为待保存
  -> 按玩家生成并发送视图更新
```

权限检查只用于桌面管理和信息显示，不用于判断桌游规则。

## 10. 牌桌权限

权限是可选的使用管理功能，不是反作弊系统。

建议提供以下角色：

- **拥有者**：创建者，可配置牌桌和授权操作。
- **参与者**：可以加入桌面并执行被授予的常规操作。
- **观战者**：可以查看允许公开的信息，但不能操作。
- **编辑者**：可以使用自由编辑操作。

权限应按操作粒度配置，而不是只提供“能打开/不能打开”：

```text
view_table
view_hidden_cards
move_cards
flip_cards
shuffle_stacks
deal_cards
edit_cards
reset_table
manage_members
```

默认配置应适合朋友之间的私密桌游；牌桌拥有者可以按需要开启完全自由编辑。

## 11. 内容模组 API 设计目标

### 11.1 注册卡牌

内容模组通过稳定的注册 API 提供卡牌定义。示意接口：

```java
CardDefinition registerCard(
    ResourceLocation id,
    CardDefinition definition
);
```

实际接口应遵循 Forge 的注册机制，并允许资源包覆盖纹理和语言文本。

### 11.2 注册牌组模板

```java
DeckTemplate registerDeck(
    ResourceLocation id,
    DeckTemplate template
);
```

牌组模板应能表达：

- 卡牌定义及数量
- 默认牌背
- 是否进入同一个牌堆
- 初始区域
- 初始桌面位置
- 是否自动洗牌

模板不包含回合、胜负或合法操作规则。

### 11.3 数据驱动优先

以下内容优先使用 JSON 或数据包表达：

- 卡牌名称和纹理
- 卡牌标签
- 牌组组成
- 默认牌背
- 区域和布局

Java 扩展点仅用于：

- 特殊渲染
- 非卡牌桌面组件
- 特殊的桌面工具
- 与其他模组集成

第一版不实现通用规则脚本引擎，避免核心 API 被某种具体桌游流程绑死。

## 12. 持久化

牌桌必须支持世界保存、区块卸载和服务器重启后的恢复。

建议使用 Minecraft 原生持久化机制保存牌桌数据，并包含：

```text
format_version
instance_id
table_config
members
permissions
regions
stacks
cards
```

### 12.1 数据版本

每次改变持久化结构时递增 `format_version`，并提供迁移策略。加载未知版本时应给出明确错误，不应静默清空牌桌。

### 12.2 保存策略

MVP 可在以下时机保存：

- 牌桌状态发生变化后标记 dirty
- 区块或世界保存时写入
- 牌桌关闭或服务器关闭时写入

不要求每个鼠标移动都立即写盘，但服务端崩溃时可能丢失尚未保存的最后状态，这一取舍需要在后续测试中确认。

## 13. 客户端交互与渲染

### 13.1 第一版交互目标

第一版优先实现稳定、可理解的二维桌面界面：

- 牌桌标题和成员列表
- 桌面区域
- 牌堆和卡牌显示
- 手牌显示
- 拖动卡牌
- 翻面和旋转
- 抽牌、发牌、洗牌按钮
- 牌桌配置和权限入口

界面可以通过牌桌方块打开，但桌面状态应属于牌桌实例，不应依赖玩家背包 GUI。

### 13.2 交互原则

- 卡牌尺寸稳定，避免拖动和悬停造成布局跳动。
- 隐藏牌只渲染牌背或数量，不在客户端普通视图中显示牌面。
- 对常见操作提供明确按钮和鼠标操作。
- 复杂操作通过上下文菜单或快捷工具栏触发。
- 错误只提示技术状态，例如“牌桌状态已更新，请重新选择对象”，不提示“违反游戏规则”。

### 13.3 世界内显示

MVP 不要求将每张卡牌作为独立实体放置在世界中。建议先使用牌桌界面渲染逻辑卡牌对象，后续再增加世界内牌桌模型或桌面预览。

这样可以降低实体同步、碰撞、区块卸载和拾取判定的复杂度。

## 14. Forge 工程建议

推荐的初始模块结构：

```text
src/main/java/<modid>/
  api/
  common/
    block/
    item/
    table/
    card/
    deck/
    persistence/
    permission/
  network/
  client/
    screen/
    render/
    input/
  data/

src/main/resources/
  META-INF/mods.toml
  assets/<modid>/
  data/<modid>/
```

具体包名应在工程初始化后统一确定。客户端渲染代码不得被服务端初始化路径加载。

## 15. MVP 范围

### 15.1 必须完成

1. Forge 工程可编译并能启动客户端和服务端。
2. 可注册并放置牌桌方块。
3. 可创建、加入和离开牌桌实例。
4. 可保存和加载牌桌状态。
5. 可注册卡牌定义。
6. 可由牌组模板创建牌堆。
7. 支持洗牌、抽牌、发牌、移动、翻面和旋转。
8. 支持至少一种隐藏手牌视图。
9. 支持基础多人同步。
10. 提供一个标准扑克牌内容模组或内置示例内容。
11. 明确支持自由编辑，至少包括创建、复制和删除卡牌中的一部分。

### 15.2 暂不实现

- 具体桌游规则
- 自动回合
- 胜负和计分
- 反作弊
- 复杂脚本引擎
- 真实物理卡牌实体
- 跨服务器牌桌
- 匹配系统和公共大厅
- 完整回放和任意撤销
- 非卡牌组件的通用物理系统

## 16. 分阶段开发计划

### 阶段 0：工程验证

- 确定 Minecraft、Forge、Java 版本。
- 创建核心模组工程和最小客户端/服务端启动配置。
- 注册测试方块和测试物品。
- 验证单人世界、专用服务器和基本网络通道。

### 阶段 1：单机桌面状态

- 实现牌桌方块和实例。
- 实现卡牌定义、实例、牌堆和区域模型。
- 实现数据保存和加载。
- 实现单人桌面 GUI。

### 阶段 2：基础多人同步

- 定义操作请求和状态更新消息。
- 服务端执行桌面状态变更。
- 按玩家生成隐藏信息视图。
- 测试多人同时移动和抽牌。

### 阶段 3：内容模组接口

- 抽出 API 模块。
- 实现卡牌和牌组注册。
- 创建标准扑克牌示例内容。
- 用 JSON 验证第三方内容注册流程。

### 阶段 4：自由编辑和桌面管理

- 增加牌桌配置。
- 增加编辑者权限。
- 实现创建、复制、删除、任意排序等自由编辑能力。
- 补充状态恢复、异常操作和版本迁移测试。

### 阶段 5：体验完善

- 优化拖动、选中、堆叠和手牌整理。
- 增加桌面布局模板。
- 增加资源包兼容性。
- 评估世界内牌桌模型和非卡牌组件。

## 17. 测试要求

### 17.1 模型测试

- 卡牌实例可以在桌面、牌堆、区域和手牌之间正确转移。
- 卡牌复制产生新的实例 ID。
- 洗牌改变顺序但不丢失或重复实例。
- 保存后重新加载，状态与保存前一致。
- 数据版本迁移不会静默清空牌桌。

### 17.2 网络测试

- 两名玩家同时操作同一牌桌时，服务端状态最终一致。
- 玩家视图不会在正常同步消息中泄露未授权牌面。
- 重复发送同一操作不会导致客户端崩溃或状态损坏。
- 玩家退出、区块卸载和服务器重启后牌桌可恢复。

### 17.3 产品测试

至少用以下场景验证“规则中立”是否成立：

- 玩家自行约定并进行一种常见扑克牌玩法。
- 玩家故意把牌放到不符合约定的位置，核心仍然允许操作。
- 玩家开启自由编辑并复制、删除和重排卡牌。
- 玩家使用没有任何规则实现的新牌组。

## 18. 风险与取舍

### 18.1 隐藏信息与客户端信任

不能把客户端视为可信的保密环境。项目只能提供正常游戏流程中的信息过滤；如果未来目标变成竞技或有裁判意义的线上桌游，需要完全不同的安全设计。

### 18.2 牌桌对象过度抽象

过早把骰子、棋子、标记物和棋盘都抽象成同一套系统，可能导致 API 复杂且难以使用。先完成卡牌对象模型，再根据实际内容模组需求抽象通用桌面对象。

### 18.3 自由编辑与常规操作的界面冲突

默认界面应让玩家快速完成常见桌游动作；自由编辑应作为桌面配置或编辑工具提供，避免所有操作都堆在主界面中。

### 18.4 状态同步成本

桌面对象和隐藏视图越复杂，网络消息越难维护。MVP 应优先使用清晰的完整/差异状态协议，并携带状态版本；只有出现实际性能问题后再优化协议。

## 19. 待定决策

以下事项不阻塞文档中的总体架构，但需要在进入对应阶段前确定：

1. 目标 Minecraft 和 Forge 版本。
2. 牌桌是单方块还是多方块尺寸。
3. 牌桌拆除时的桌面状态处理方式。
4. 手牌是否始终属于牌桌，还是允许转为独立物品。
5. 牌桌是否允许多个独立牌局区域。
6. 网络同步采用完整快照、差异更新，还是两者结合。
7. 是否需要操作撤销，以及哪些操作允许撤销。
8. 是否把标准扑克牌作为独立模组，还是作为示例内容随核心项目提供。
9. 是否在 MVP 后支持骰子、棋子和计分标记。

## 20. 一句话验收标准

如果玩家可以在 Minecraft 中放置一张牌桌，加载任意内容模组提供的牌组，与好友共享桌面、看到各自应看到的信息，并通过移动、翻面、洗牌、抽牌和自由编辑完成一局由玩家自行约定的桌游，那么核心方向就达到了 MVP 目标。
