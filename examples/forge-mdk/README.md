# Forge MDK Reference

This directory preserves the original Forge MDK sample classes for reference only.

Files here are outside `src/main/java`, so Gradle does not compile them and Forge does not register their example block, item, creative tab, events, or configuration. The production mod must not import classes from this directory.
